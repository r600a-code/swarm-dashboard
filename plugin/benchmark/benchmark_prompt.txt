Call the swarm_dispatch tool exactly once with argument tasks set to this JSON array, passing it verbatim (do NOT edit or rephrase the prompts; do NOT pass fewer tasks):

[
  {"id":"t1-fast","kind":"fast","maxTokens":320,"prompt":"Compute 17 * 23 and reply with ONLY the integer, nothing else."},
  {"id":"t2-reasoning","kind":"reasoning","maxTokens":1024,"prompt":"A bat and a ball cost $1.10 in total. The bat costs $1.00 more than the ball. How much does the ball cost? Reply with ONLY the dollar amount, for example 0.05."},
  {"id":"t3-coding","kind":"coding","maxTokens":1024,"prompt":"Write a Python function is_prime(n) that returns True if n is prime, else False. Output ONLY the code, no explanation."},
  {"id":"t4-general","kind":"general","maxTokens":512,"prompt":"Translate this exact English sentence to Chinese: The swarm router assigns each task to the most suitable model. Reply with ONLY the Chinese translation."},
  {"id":"t5-reasoning2","kind":"reasoning","maxTokens":1024,"prompt":"If it takes 5 machines 5 minutes to make 5 widgets, how long would 100 machines take to make 100 widgets? Reply with ONLY the number of minutes."}
]

After the tool returns, output ONLY the raw JSON object the tool returned (it has a routed summary, assignments, and a results array where each entry has the chosen model and that subagent's real answer), with no extra commentary, then stop.